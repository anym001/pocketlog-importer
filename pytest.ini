[pytest]
pythonpath = .
testpaths = tests
; The default run stays the fast unit suite; contract tests need Docker and
; are selected explicitly with `pytest -m integration`.
addopts = -m "not integration"
markers =
    integration: contract tests against a real PocketLog container
filterwarnings =
    ignore::DeprecationWarning
